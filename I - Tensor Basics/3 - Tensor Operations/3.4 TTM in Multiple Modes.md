# 3.4 TTM in Multiple Modes

When we compute the TTMs, the order of the multiplications does not matter.
Indeed,
> **Proposition (TTM Order)** <br>
> *Let* $\mathbf{x}$ *be a tensor such that mode* $k$ *is of size* $m$ *and mode* $l$ *is of size* $n$*. *Then, for any* $\mathbf{U} \in \mathbb{R}^{q \times m}$ *and* $\mathbf{V} \in \mathbb{R}^{r \times n}$ *with* $k \neq l$, *whe have,* <br>
> $( \mathbf{x} \times_k \mathbf{U}) \times_l \mathbf{V} = (\mathbf{x} \times_l \mathbf{V}) \times_k \mathbf{U}.$



## 3.4.1 Multi-TTM for 3-way Tensors

We learnt that for a tensor $\mathbf{x} \in \mathbb{R}^{m \times n \times p}$, the expression is
$\mathbf{y} = \mathbf{x} \times_1 \mathbf{U} \times_2 \mathbf{V} \times_3 \mathbf{W}$ <br> with $\mathbf{U} \in \mathbb{R}^{q \times m}, \mathbf{V} \in \mathbb{R}^{r \times n} \text{ and } \mathbf{W} \in \mathbb{R}^{s \times p}$. <br>
Then the size of $\mathbf{y}$ became $q \times r \times s$. <br>
Each matrix transforms the tensor along one mode: mode-1, mode-2, and mode-3 respectively. <br>
This operation is useful to project or compress data along different dimensions such as *time*, *location*, or *season* for example.

Let take a tensor $\mathbf{x} \in \mathbb{R}^{ \text{cities} \times \text{days} \times \text{sensors}}$:

```math
\begin{aligned}
\text{We have } m = 10 \text{ cities; } n = 365 \text{ days; } p = 5 \text{ sensors.} \\[8pt]
\text{Then Mode-1 (cities):} \mathbf{U} \in \mathbb{R}^{3 \times 10} \text{ -> 10 cities in 3 states} \\[8pt]
\text{Mode-2 (days):} \mathbf{V} \in \mathbb{R}^{12 \times 365} \text{ -> 365 days in 12 months} \\[8pt]
\text{Mode-3 (sensors):} \mathbf{W} \in \mathbb{R}^{2 \times 5} \text{ -> 5 sensors in 2 latent variables} \\[8pt]
\text{We can now express } \mathbf{y} \in \mathbb{R}^{3 \times 12 \times 2} \text{ as aggregated measurements by region, by month, and by latent sensor type.} \end{aligned}
```

## 3.4.2 TTM with Multiple Matrices for *d*-way Tensors

## 3.4.3 Efficient Multi-TTM Computation

<br><br>

## Exercises

> **Exercise 3.17** <br>
> Prove Proposition 1. As a hint, you can argue via Exercise 3.12 on permutation of the modes and TTMs, that if suffices to show $\mathbf{y} \times_1 \mathbf{U} \times_2 \mathbf{V} = \mathbf{y} \times_2 \mathbf{V} \times_1 \mathbf{U},$ where $\mathbf{y} \in \mathbb{R}^{m \times n \times p}$ with $p$ being the product of the remaining inidces.

### Solution - Exercise 3.17
