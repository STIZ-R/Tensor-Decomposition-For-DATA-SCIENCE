# 3.4 TTM in Multiple Modes

When we compute the TTMs, the order of the multiplications does not matter.
Indeed,
> **Proposition (TTM Order)** <br>
> *Let* $\mathbf{x}$ *be a tensor such that mode* $k$ *is of size* $m$ *and mode* $l$ *is of size* $n$*. *Then, for any* $\mathbf{U} \in \mathbb{R}^{q \times m}$ *and* $\mathbf{V} \in \mathbb{R}^{r \times n}$ *with* $k \neq l$, *whe have,* <br>
> $( \mathbf{x} \times_k \mathbf{U}) \times_l \mathbf{V} = (\mathbf{x} \times_l \mathbf{V}) \times_k \mathbf{U}.$



## 3.4.1 Multi-TTM for 3-way Tensors

We learnt that for a tensor $\mathbf{x} \in \mathbb{R}^{m \times n \times p}$, the expression is
$\mathbf{y} = \mathbf{x} \times_1 \mathbf{U} \times_2 \mathbf{V} \times_3 \mathbf{W}$ <br> with $\mathbf{U} \in \mathbb{R}^{q \times m}, \mathbf{V} \in \mathbb{R}^{r \times n} \text{ and } \mathbf{W} \in \mathbb{R}^{s \times p}$. <br>
Then the size of $\mathbf{y}$ became $q \times r \times s$. <br>
Each matrix transforms the tensor along one mode: mode-1, mode-2, and mode-3 respectively. <br>
This operation is useful to project or compress data along different dimensions such as *time*, *location*, or *season* for example.

<br>

Let take a tensor $\mathbf{x} \in \mathbb{R}^{ \text{cities} \times \text{days} \times \text{sensors}}$: <br>
We have $m = 10$ cities; $n = 365$ days; $p = 5$ sensors. <br>
Then **Mode-1** (cities): $\mathbf{U} \in \mathbb{R}^{3 \times 10}$ -> 10 cities in 3 states. <br>
**Mode-2** (days): $\mathbf{V} \in \mathbb{R}^{12 \times 365}$ -> 365 days in 12 months. <br>
**Mode-3** (sensors): $\mathbf{W} \in \mathbb{R}^{2 \times 5}$ -> 5 sensors in 2 latent variables. <br>
We can now express  $\mathbf{y} \in \mathbb{R}^{3 \times 12 \times 2}$ as aggregated measurements by *region*, by *month*, and by *latent sensor type*.

We can use the Kronecker product to express the **vectorization** and **unfoldings** as follows: <br>
> **Proposition (Mutli-TTM Vectorization and Mode-*k* Unfolding, 3-way)** <br>
> *Let* $\mathbf{x} \in \mathbb{R}^{m \times n \times p}, \mathbf{U} \in \mathbb{q \times m}, \mathbf{V} \in \mathbb{R}^{r \times n} \text{ and } \mathbf{W} \in \mathbb{R}^{s \times p}$ *so that* $\mathbf{y} \in \mathbb{R}^{q \times r \times s}.$ <br>
> *The followoing are equivalent:* <br>
> $\mathbf{y} = \mathbf{x} \times_1 \mathbf{U} \times_2 \mathbf{V} \times_3 \mathbf{W},$ <br>
> $\mathrm{vec(\mathbf{y})} = (\mathbf{W} \otimes \mathbf{V} \otimes \mathbf{U}) \mathrm{vec(\mathbf{x})},$ <br>
> $\mathbf{Y_{(1)}} = \mathbf{UX_{(1)}}(\mathbf{W} \otimes \mathbf{V})^T,$ <br>
> $\mathbf{Y_{(2)}} = \mathbf{VX_{(2)}}(\mathbf{W} \otimes \mathbf{U})^T,$ <br>
> $\mathbf{Y_{(3)}} = \mathbf{WX_{(3)}}(\mathbf{V} \otimes \mathbf{U})^T.$

## 3.4.2 TTM with Multiple Matrices for *d*-way Tensors

We can now explain the TTM for a *d*-way tensor with the following formula: <br>
$\mathbf{y} = \mathbf{x} \times_1 \mathbf{U_1} \times_2 \mathbf{U_2} \dots \times_d \mathbf{U_d}$ with a tensor $\mathbf{x} \in \mathbb{R}^{n_1 \times n_2 \times \dots \times n_d}$ and matrices $\mathbf{U_k} \in \mathbb{R}^{m_k \times n_k}$ for all $k \in [d]$.

We'll see that the multi-TTM operation can be expressed using the *cavariant* and the *contravariant* notation as follows:
$\mathbf{y} = (\mathbf{U_1},\mathbf{U_2}, \dots, \mathbf{U_d})\mathbf{x} \text{ or } \mathbf{y} = \mathbf{x}(\mathbf{U_1^T},\mathbf{U_2^T}, \dots, \mathbf{U_d^T}).$

> **Proposition (Multi-TTM Vectorization and Mode_*k* Unfolding)** <br>
> *The following are equivalent for* $\mathbf{x} \in \mathbb{R}^{n_1 \times n_2 \times \dots \times n_d}:$ <br>
> $\mathbf{y} = \mathbf{x} \times_1 \mathbf{U_1} \times_2 \mathbf{U_2} \dots \times_d \mathbf{U_d},$ <br>
> $\mathrm{vec(\mathbf{y})} = (\mathbf{U_d} \otimes \mathbf{U_{d-1}} \otimes \dots \otimes \mathbf{U_1})\mathrm{vec(\mathbf{x})},$ <br>
> $\mathbf{Y_{(k)}} = \mathbf{U_k}\mathbf{X_{(k)}}(\mathbf{U_d} \otimes \dots \otimes \mathbf{U_{k+1}} \otimes \mathbf{U_{k-1}} \otimes \dots \otimes \mathbf{U_1})^T \text{ for each } k \in [d]$.

> **Proposition (Multi-TTM Unfolding)** <br>
> *Let* $\mathbf{x} \in \mathbb{R}^{n_1 \times n_2 \times \dots \times n_d}$, $\mathbf{U_k} \in \mathbb{R}^{m_k \times n_k}$ *for all* <br>
> $\mathfrak{R} = (r_1, r_2, \dots, r_{\delta}) \text{ and } \mathfrak{C} = (c_1, c_2, \dots, c_{d - \delta}).$ <br>
> *Then* <br>
> $\mathbf{y} = \mathbf{x} \times_1 \mathbf{U_1} \times_2 \mathbf{U_2} \dots \times_d \mathbf{U_d} \in \mathbb{R}^{m_1 \times m_2 \times \dots \times m_d}$ <br>
> *if and only if* <br>
> $\mathbf{Y_{(\mathfrak{R} \times \mathfrak{C})}}$

## 3.4.3 Efficient Multi-TTM Computation

<br><br>

## Exercises

> **Exercise 3.17** <br>
> Prove Proposition 1. As a hint, you can argue via Exercise 3.12 on permutation of the modes and TTMs, that if suffices to show $\mathbf{y} \times_1 \mathbf{U} \times_2 \mathbf{V} = \mathbf{y} \times_2 \mathbf{V} \times_1 \mathbf{U},$ where $\mathbf{y} \in \mathbb{R}^{m \times n \times p}$ with $p$ being the product of the remaining inidces.

### Solution - Exercise 3.17
